html
head
body
    br
    br
    h2=msge
    form(method="POST", action="/nextStep/menu", enctype="multipart/form-data")
        button(type="submit", name="ml") Back
    form(method="POST", action="/nextStep/quit", enctype="multipart/form-data")
        button(type="submit", name="sd") Exit